services:
  - type: web
    name: placement-prediction-ml
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python retrain_models.py
    startCommand: gunicorn app:app